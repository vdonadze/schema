kind: SettingsFile
metadata:
  name: infra-mgr-config
spec:
  cradle:
    keyspace: "testsmokewdsadsa"
  k8s-propagation: rule
  bookConfig:
    defaultBook: "testbook"
  th2BoxConfig:
    mqRouter:
      connectionTimeout: -1
      connectionCloseTimeout: 10000
      maxRecoveryAttempts: 5
    grpcRouter:
      workers: 5
    cradleManager:
      connectionCloseTimeout: 1000
    logging:
      logLevelTh2: TRACE
      logLevelRoot: DEBUG
